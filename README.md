# MyBlog
The build.py should generate the site through string replacement function. The site still has a lot of commented out code which is messy, but there is decent spacing which makes it navigable. I'm keeping this way for a bit long while I finalize stylistic choices. 
